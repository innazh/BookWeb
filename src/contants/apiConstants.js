export const API_BASE_URL = process.env.REACT_APP_SERVER_URL; //assign the env var with the address of the server to the global var and export it
export const ACCESS_TOKEN_NAME = 'login_access_token';